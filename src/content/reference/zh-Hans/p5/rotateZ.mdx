---
title: rotateZ
module: 变形
submodule: 变形
file: src/core/transform.js
description: >
  <p>在 WebGL 模式下，围绕 z 轴旋转坐标系。</p>

  <p>参数 <code>angle</code> 是旋转的角度。例如，调用 <code>rotateZ(1)</code> 会使坐标系围绕 z 轴旋转 1 弧度。<code>rotateZ()</code> 根据当前的 <a href="/reference/p5/angleMode">angleMode()</a> 解释角度值。</p>

  <p>默认情况下，转换会累积。例如，连续调用两次 <code>rotateZ(1)</code> 与调用一次 <code>rotateZ(2)</code> 效果相同。可以使用 <a href="/reference/p5/push">push()</a> 和 <a href="/reference/p5/pop">pop()</a> 函数将转换隔离到不同的绘图组中。</p>

  <p>注意：转换会在绘制循环的开始处重置。在 <a href="/reference/p5/draw">draw()</a> 函数中调用 <code>rotateZ(1)</code> 不会导致形状旋转。</p>
line: 743
isConstructor: false
itemtype: method
alt: 3d box rotating around the z axis.
example:
  - |-

    <div modernizr='webgl'>
    <code>
    function setup() {
      createCanvas(100, 100, WEBGL);
      camera(0, 0, 50*sqrt(3), 0, 0, 0, 0, 1, 0);
      perspective(PI/3, 1, 5*sqrt(3), 500*sqrt(3));
    }
    function draw() {
      background(255);
      rotateZ(millis() / 1000);
      box();
    }
    </code>
    </div>
class: p5
params:
  - name: angle
    description: >
      <p>当前 <a href="#/p5/angleMode">angleMode()</a> 中的旋转角度。</p>
    type: 数字
chainable: true
---


# rotateZ
